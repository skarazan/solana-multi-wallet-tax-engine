# solana-multi-wallet-tax-engine
Accurate multi-wallet Solana tax + PnL engine. Rebuilds cost basis from raw transactions, tracks FIFO lots across wallets, handles swaps correctly, and exports tax-ready CSV reports. No price guessing, only real on-chain data.
